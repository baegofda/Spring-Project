<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Member">
	<select id="MemberAll" parameterType="Member" resultType="Member">
      select * from (select rownum rn, a.* from (select * from Member
    	 order by mId) a) where rn between #{start} and	 #{end}   
    </select>
    <select id="total" resultType="int">
     	select count(*) from Member
    </select> 
    <insert id="joinMember" parameterType="Member">
    	insert into member values(#{mId}, #{mNick}, #{mName}, #{mPw}, #{mAddr}, #{mAddr2}, #{mTel}, 1, #{mSweet}, sysdate, #{mMail}, #{mSms})
    </insert> 
    <select id="jList">
    	select *
    	from member
    </select>
    <select id="idCheck" parameterType="java.lang.String" resultType="java.lang.String">
    	select mId from member where mId=#{mId}
    </select>
</mapper>